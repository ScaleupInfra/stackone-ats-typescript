# This is a template for your environment configuration file.
# Follow the instructions below to set up your environment variables correctly.

# Base URL for backend API calls
# Replace the example URL "http://localhost:3001" with the actual base URL of your backend API.
# Example: If your backend is hosted at "https://api.yourdomain.com", replace the value as shown below:
# REACT_APP_API_ATS_URL="https://api.yourdomain.com"
REACT_APP_API_ATS_URL="http://localhost:3001"

# URL for session-related API calls
# Similar to the above, replace this with the actual URL for session management if it's different.
# Ensure the correct URL format is used, for example:
# REACT_APP_API_SESSION_URL="https://auth.yourdomain.com"
REACT_APP_API_SESSION_URL="http://localhost:3001"

# Example of another environment variable:
# Uncomment and replace "https://example.com" with the actual URL if your project requires additional APIs.
# Example:
# REACT_APP_ANOTHER_API_URL="https://api.anotherdomain.com"

# IMPORTANT:
# 1. Copy the contents of this file into a new file named `.env` in the root directory of your project.
# 2. Replace the example values with your actual configuration values.
# 3. Keep your `.env` file secure and ensure it is not tracked by version control (e.g., using a `.gitignore` file).

# To copy this template to a `.env` file and edit it, use the following command in your terminal:
# cp .env.example .env

# After copying, open the `.env` file and replace the example values with your actual data.
